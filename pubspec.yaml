name: evergreen_lifechurch_todo
description: Evergreen todo app for life.church interview process
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: 3.24.3 # Make sure matches pipeline / build servers
  # TODO: Eventually use puro or fvm or flutterw or scripted versions.

dependencies:

  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
  generate: true